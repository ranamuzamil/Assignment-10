// 22. Write a js program to display all array elements using function.
function displayArray(arr) {
    for (let i = 0; i < arr.length; i++) {
      console.log(arr[i]);
    }
  }
  //i.e
  displayArray([1, 2, 3, 4, 5]);
  displayArray(["what", "how", "where"]);
  